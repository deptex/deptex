# Framework-pattern audit

Resolves the side-finding from `cbecc39` (pattern-syntax validator landing): the worry that ~5% of bundled framework-spec patterns might be unmatchable by the engine's actual DSL.

Generated by `depscanner/scripts/framework-pattern-audit.ts`. Classifies every pattern in `depscanner/src/taint-engine/framework-models/*.yaml` against the live engine matchers (`matchesCallPattern`, `matchSourcePattern`, `sanitizerNameProbe`) AND the per-language IR's known source-text emission rules.

## Method

Three matcher paths are reproduced verbatim:

1. **`matchesCallPattern(pattern, calleeText)`** (`propagate-core.ts:420`) — strips trailing `(*)`, then accepts wildcard-receiver `*.foo` / `*->foo` / `*::foo` (suffix match), exact `calleeText === p`, or last-segment fallback `calleeText === p.split('.').pop()`.
2. **`matchSourcePattern(text, specs)`** (`propagate-core.ts:375`) — for non-call sources: `pattern.endsWith(".*")` triggers prefix-match (also matches `prefix.X` and `prefix[X]`); else `text === pattern`.
3. **`sanitizerNameProbe(pattern)`** (`fp-filter.ts:727`) — separate regex-grep over flow source lines for sanitizer detection (`\bstem\s*\(` for `(*)` patterns, `\bprefix\.` for `.*`, `\bpattern\b` otherwise).

Each pattern is tested against every plausible IR-emitted input. A pattern is `confirmed-dead` ONLY if no matcher path can fire under any input; otherwise it lands in one of the live buckets:

- `dsl-clean-exact` — pattern matches via the canonical exact-text path (e.g. `req.body`, `res.send(*)`, `sqlx::query(*)`).
- `dsl-clean-wildcard` — wildcard receiver path (`*.method(*)`, `*->method(*)`).
- `dsl-clean-prefix` — `.*` prefix source (`req.body.*`, `Query.0.*`).
- `language-specific-matcher` — only fires because a per-language IR (csharp/ir.ts in practice) synthesizes a sourceText literally equal to the pattern. Verified by reading the IR.
- `redundant-with-prefix` — exact-source pattern with a literal subscript (`params[*]`) that the engine's string matcher never sees in IR text, BUT a sibling `.*` prefix in the same yaml subsumes its intended coverage. Live but redundant.

**Total patterns audited:** 1633

## Per-language breakdown

| Language | Patterns |
|---|---|
| csharp | 93 |
| go | 133 |
| java | 89 |
| js | 180 |
| php | 270 |
| python | 331 |
| ruby | 281 |
| rust | 256 |

## Per-framework breakdown

| Framework | Patterns |
|---|---|
| actix-web | 89 |
| aspnet-core | 38 |
| axum | 80 |
| django | 109 |
| dotnet-stdlib | 55 |
| echo | 21 |
| express | 17 |
| fastapi | 113 |
| fastify | 12 |
| flask | 109 |
| gin | 29 |
| go-stdlib | 83 |
| hono | 17 |
| java-stdlib | 56 |
| laravel | 145 |
| nestjs | 5 |
| nextjs | 16 |
| node-stdlib | 113 |
| rails | 150 |
| rust-stdlib | 87 |
| sinatra | 131 |
| spring-boot | 33 |
| symfony | 125 |

## Classification counts

| Class | Count | % |
|---|---|---|
| `dsl-clean-exact` | 1330 | 81.45% |
| `dsl-clean-prefix` | 111 | 6.80% |
| `dsl-clean-wildcard` | 178 | 10.90% |
| `language-specific-matcher` | 7 | 0.43% |
| `redundant-with-prefix` | 7 | 0.43% |

## Top findings

### `confirmed-dead`: 0

No bundled pattern fails every matcher path. The `cbecc39` agent's "~5% unmatchable" estimate was conservative: every shape they flagged turns out to be live via one of:

- the per-language IR emitting the source text verbatim (csharp `[FromBody]`, `@HttpRouteParameter`)
- the rust IR preserving `::` in calleeText (`sqlx::query`, `Net::HTTP` would survive too — but no ruby spec uses `::` in a `pattern:` field, only in `description:`)
- chained-call patterns (`req.uri().path()`) matching exactly because the rust IR emits `req.uri().path` as calleeText for chained method calls
- whitespace-containing patterns (`new ProcessBuilder`) matching because the java IR emits `new ProcessBuilder` (with space) as calleeText for `object_creation_expression`
- backtick patterns (`` `(*) ``, `` Kernel.`(*) ``) matching the literal Ruby method name `` ` ``
- the `last`-segment fallback (`p.split(".").pop()`) — but no audited pattern depends ONLY on this path; every dotted pattern matches the primary `calleeText === p` path against at least one realistic input.

### `dsl-clean-exact` — 1330 patterns

| File | Kind | Pattern | Rationale |
|---|---|---|---|
| actix-web.yaml | source | `web::Query.into_inner(*)` | exact dotted path "web::Query.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `web::Json.into_inner(*)` | exact dotted path "web::Json.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `web::Path.into_inner(*)` | exact dotted path "web::Path.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `web::Form.into_inner(*)` | exact dotted path "web::Form.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `Query.into_inner(*)` | exact dotted path "Query.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `Json.into_inner(*)` | exact dotted path "Json.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `Path.into_inner(*)` | exact dotted path "Path.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `Form.into_inner(*)` | exact dotted path "Form.into_inner" matches calleeText verbatim |
| actix-web.yaml | source | `req.headers().get(*)` | exact dotted path "req.headers().get" matches calleeText verbatim |
| actix-web.yaml | source | `req.cookie(*)` | exact dotted path "req.cookie" matches calleeText verbatim |

_1320 more in `framework-pattern-audit.json`._


### `dsl-clean-wildcard` — 178 patterns

| File | Kind | Pattern | Rationale |
|---|---|---|---|
| actix-web.yaml | source | `*.into_inner(*)` | wildcard receiver matches any callee ending in ".into_inner" |
| actix-web.yaml | sink | `*.query(*)` | wildcard receiver matches any callee ending in ".query" |
| actix-web.yaml | sink | `*.execute(*)` | wildcard receiver matches any callee ending in ".execute" |
| actix-web.yaml | sink | `*.query_as(*)` | wildcard receiver matches any callee ending in ".query_as" |
| actix-web.yaml | sink | `*.fetch_all(*)` | wildcard receiver matches any callee ending in ".fetch_all" |
| actix-web.yaml | sink | `*.arg(*)` | wildcard receiver matches any callee ending in ".arg" |
| actix-web.yaml | sink | `*.args(*)` | wildcard receiver matches any callee ending in ".args" |
| actix-web.yaml | sanitizer | `*.file_name(*)` | wildcard receiver matches any callee ending in ".file_name" |
| actix-web.yaml | sanitizer | `*.parse(*)` | wildcard receiver matches any callee ending in ".parse" |
| aspnet-core.yaml | sink | `*.WriteAsync(*)` | wildcard receiver matches any callee ending in ".WriteAsync" |

_168 more in `framework-pattern-audit.json`._


### `dsl-clean-prefix` — 111 patterns

| File | Kind | Pattern | Rationale |
|---|---|---|---|
| actix-web.yaml | source | `params.*` | prefix "params" matches via .* prefix path |
| aspnet-core.yaml | source | `Request.Query.*` | prefix "Request.Query" matches via .* prefix path |
| aspnet-core.yaml | source | `Request.Form.*` | prefix "Request.Form" matches via .* prefix path |
| aspnet-core.yaml | source | `Request.Cookies.*` | prefix "Request.Cookies" matches via .* prefix path |
| aspnet-core.yaml | source | `Request.Headers.*` | prefix "Request.Headers" matches via .* prefix path |
| aspnet-core.yaml | source | `Request.RouteValues.*` | prefix "Request.RouteValues" matches via .* prefix path |
| aspnet-core.yaml | source | `HttpContext.Request.*` | prefix "HttpContext.Request" matches via .* prefix path |
| aspnet-core.yaml | source | `this.Request.*` | prefix "this.Request" matches via .* prefix path |
| axum.yaml | source | `Query.0.*` | prefix "Query.0" matches via .* prefix path |
| axum.yaml | source | `Json.0.*` | prefix "Json.0" matches via .* prefix path |

_101 more in `framework-pattern-audit.json`._


### `language-specific-matcher` — 7 patterns

| File | Kind | Pattern | Rationale |
|---|---|---|---|
| aspnet-core.yaml | source | `[FromBody]` | csharp/ir.ts emits sourceText `[FromBody]`/etc verbatim for [Attribute]-decorated parameters |
| aspnet-core.yaml | source | `[FromQuery]` | csharp/ir.ts emits sourceText `[FromBody]`/etc verbatim for [Attribute]-decorated parameters |
| aspnet-core.yaml | source | `[FromRoute]` | csharp/ir.ts emits sourceText `[FromBody]`/etc verbatim for [Attribute]-decorated parameters |
| aspnet-core.yaml | source | `[FromForm]` | csharp/ir.ts emits sourceText `[FromBody]`/etc verbatim for [Attribute]-decorated parameters |
| aspnet-core.yaml | source | `[FromHeader]` | csharp/ir.ts emits sourceText `[FromBody]`/etc verbatim for [Attribute]-decorated parameters |
| aspnet-core.yaml | source | `[FromServices]` | csharp/ir.ts emits sourceText `[FromBody]`/etc verbatim for [Attribute]-decorated parameters |
| aspnet-core.yaml | source | `@HttpRouteParameter` | csharp/ir.ts emits sourceText `@HttpRouteParameter` for parameters of [HttpGet]/[HttpPost]/etc methods |

### `redundant-with-prefix` — 7 patterns

| File | Kind | Pattern | Rationale |
|---|---|---|---|
| rails.yaml | source | `params[*]` | subscript/numeric-segment source "params[*]" never matches IR text literally; sibling "params.*" already covers all real inputs |
| rails.yaml | source | `cookies[*]` | subscript/numeric-segment source "cookies[*]" never matches IR text literally; sibling "cookies.*" already covers all real inputs |
| rails.yaml | source | `session[*]` | subscript/numeric-segment source "session[*]" never matches IR text literally; sibling "session.*" already covers all real inputs |
| sinatra.yaml | source | `params[*]` | subscript/numeric-segment source "params[*]" never matches IR text literally; sibling "params.*" already covers all real inputs |
| sinatra.yaml | source | `env[*]` | subscript/numeric-segment source "env[*]" never matches IR text literally; sibling "env.*" already covers all real inputs |
| sinatra.yaml | source | `cookies[*]` | subscript/numeric-segment source "cookies[*]" never matches IR text literally; sibling "cookies.*" already covers all real inputs |
| sinatra.yaml | source | `session[*]` | subscript/numeric-segment source "session[*]" never matches IR text literally; sibling "session.*" already covers all real inputs |

## Recommendations

1. **No cleanup commit warranted.** Zero `confirmed-dead` patterns; nothing safe to delete.
2. **Keep the 7 `redundant-with-prefix` Ruby/Sinatra `params[*]` / `cookies[*]` / `session[*]` / `env[*]` sources as documentation.** They're subsumed by sibling `params.*` prefix sources. Removing them would shrink the spec by 7 lines but lose authorial intent — these YAML entries are valuable as a self-documenting list of subscript-style accesses readers expect to be tainted. They cost nothing at runtime (the loop short-circuits on the first match in `matchSourcePattern`).
3. **Keep the 7 `language-specific-matcher` C# attribute sources.** Verified live by reading `csharp/ir.ts` lines 65-72, 132-145, and 725-744.
4. **Tighter validator is unsafe.** A grammar-strict validator that rejects whitespace, brackets, or `::` would regress 246+ Rust `::` patterns, 8 Java `new TypeName` constructor patterns, 7 C# attribute sources, 3 Rust chained-call sources (`req.uri().path()`-style), 2 Ruby backtick sinks, and the entire `params[*]` redundant-doc family. Pre-flight `pattern-syntax.ts` already catches the truly malformed cases (unbalanced delimiters, control chars, embedded newlines); that's the right level for the gate.
5. **Reproducibility:** run `npx tsx scripts/framework-pattern-audit.ts` from `depscanner/` to regenerate this audit. Output lands at `docs/framework-pattern-audit.{md,json}`. Keep the script in tree so future spec additions can re-verify.
