# symfony / unreachable — CVE-2024-50340

- **Vulnerable dep:** `symfony/runtime 6.3.0` (declared, no #[Route] attribute).
- **Why unreachable:** controller has no annotated route methods.
- **Expected verdict:** `module`.
