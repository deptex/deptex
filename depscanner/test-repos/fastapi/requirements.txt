fastapi==0.65.0
sqlalchemy==1.3.0
uvicorn==0.13.4
# Reachable dependency-CVE seed: PyYAML 5.3.1 → CVE-2020-14343 (yaml.load RCE,
# Text4Shell-style). The /config/import route flows the request body into
# yaml.load, so the taint engine confirms this dep CVE as reachable.
PyYAML==5.3.1
