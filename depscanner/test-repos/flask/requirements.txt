flask==1.0.4
werkzeug==0.15.3
# Reachable dependency-CVE seed: PyYAML 5.3.1 → CVE-2020-14343 (yaml.load RCE,
# Text4Shell-style). The /deser/yaml route flows request.form into yaml.load,
# so the taint engine confirms this dep CVE as reachable.
PyYAML==5.3.1
